# Core application dependencies for NetSpeedMeter
# matplotlib and its heavy dependency chain (numpy, pillow, scipy) are
# no longer required — the history graph window has been removed.

PyQt6
pywin32
psutil
wmi
numpy          # used by widget_state / db_utils for speed history
